# adapters/claude — local adapter config (overrides config.py defaults).

# Experimental Feature Graph IR tool (submit_feature_graph) — opt-in kill switch (gate layer 1).
# true = enable it for this test user. Takes effect after an adapter reconnect.
SOLIDPILOT_ENABLE_IR=false
